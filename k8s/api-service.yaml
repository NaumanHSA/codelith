apiVersion: v1
kind: Service
metadata:
  name: docany-api
  namespace: document-anything
  labels:
    app: docany-api
spec:
  selector:
    app: docany-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP

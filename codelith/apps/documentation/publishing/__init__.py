"""
Publishing: the site as a served URL rather than a downloaded archive.

Everything here is the documentation app's own. The base knows nothing about
publishing, and `tests/unit/test_module_isolation.py` keeps it that way.
"""
